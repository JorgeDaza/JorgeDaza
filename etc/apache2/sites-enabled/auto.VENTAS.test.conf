<VirtualHost *:80> 
    DocumentRoot "C:/laragon/www/VENTAS"
    ServerName VENTAS.test
    ServerAlias *.VENTAS.test
    <Directory "C:/laragon/www/VENTAS">
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>

# If you want to use SSL, enable it by going to Menu > Apache > SSL > Enabled
